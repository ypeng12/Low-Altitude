# Install in an isolated environment. NumPy is capped because the repository's
# current global scientific stack contains NumPy-1.x binary extensions.
numpy>=1.24,<2
pandas>=2.0,<3
scipy>=1.10,<1.14
scikit-learn>=1.3,<1.7
umap-learn>=0.5.7,<0.6
# Newer Numba releases no longer publish macOS x86_64 wheels; keep the
# last compatible wheel range so setup remains reproducible on this project host.
numba>=0.59,<0.62
sentence-transformers[onnx]>=3.4,<4
transformers>=4.41,<4.49
optimum[onnxruntime]>=1.23,<2
torch>=2.1,<3
fasttext-wheel>=0.9.2,<0.10
matplotlib>=3.7,<3.10
pytest>=8,<9
